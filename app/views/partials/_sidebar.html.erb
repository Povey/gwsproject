<% if params[:controller] == 'about' %>
<div class="well">
  <h3>Find Us!</h3>
  <ul>
    <li>Granite Worktops Sussex</li>
    <li>Rock Lane</li>
    <li>Hastings</li>
    <li>East Sussex</li>
  </ul>
</div>
<% end %>

<% if params[:controller] == 'samples' && params[:action] == 'index' || params[:action] == 'show' %>

<div class="list-group">
    <% @categories.each do |category| %>
        <%= link_to category.name, category_path(category.id), class:'list-group-item' %>
    <%end%>
</div>

<% end %>
